# introduction commands to see whats all about and the basic elements

Directory Structure
Linux uses a single root directory "/"
Everything (files & folders) starts from "/"
bash - /
COMMANDS ;

ls                         -- 📃 List Directory Contents 

ls-a                       -- List all files (including hidden)

ls-l                       -- List with details

more FILENAME              -- more is the command to see a particular file 📄 View File Contents

cat FILENAME               -- same as more an alternative command

mkdir directory_name       -- make a new directive 📂

mkdir dir1 dir2 dir3       -- for multiple directive making

mv file.txt folder/        -- mv is the command used to move 

mv oldname.txt newname.txt -- move also used as a rename command

cd folder_name             -- to change directory to any particular directory that mentioned after the cd command

cd ..                      -- this command is used for going back one level (can be consider as a previos button of kind)

cd ~                       -- this command is used to go back to home directory

pwd                        -- this command is used for printing current location that you are in

cp  source destination     -- cp command is basicaly copy and have to mention were is the copy content and were do you want to put it 

rm file.txt                -- is used to remove or delete a file

rmdir folder_name          -- for removing or deleting directory

rm -r folder_name          -- for removing directory with files

ls -l                      -- to see permissions and file details
                                 r   → read
                                 w   → write
                                 x   → execute
                                 -   → permission denied

chmod                      -- changes file permission modes.
                               chmod [who][operator][permission] filename

                                Who:

                                 u: user (owner)
                                 g: group
                                 o: others 
                                 a: all (user + group + others)

                                Operators:

                                 +: add permission 
                                 -: remove permission

                                Permissions:

                                 r: read
                                 w: write
                                 x: execute

chmod g+w notes.txt        -- Give write permission to group 
chmod u-w notes.txt        -- Remove write permission from owner
chmod a-rwx secret.txt     -- Remove all permissions from everyone
chmod a+rwx public.txt     -- Give all permissions to everyone

Wildcard

ls *                       -- Apply a command to all files in the current directory

chmod g+w *                -- Change permissions for all files

ls *ing                    -- Match files ending with a specific pattern

~                          -- shortcut that represents your home folder

man ls                     -- provides comprehensive documentation including the command's name,
                              synopsis, description, options, examples, and related commands

man man                    -- to learn about the man command itself

find                       -- use to find file(you have to guide it to directory for effeciency)
                              ~ -name "poem*"
                              ~ = home directory/user directory
                              -name = characterstic of file is it name,number,size,date...
                              "poem*" = approx file name

cat                        --  (concatenate)  combining files 
                               cat file1.txt file2.txt > combined.txt









